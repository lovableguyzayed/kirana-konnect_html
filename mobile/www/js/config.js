// ============================================================================
//  Kirana Konnect - Android app configuration
// ============================================================================
//  Set this to your deployed backend URL (the Render web service URL).
//  This is the ONLY value you must change to point the app at your server.
//
//  Example: "https://kirana-konnect.onrender.com"
//
//  If you use a custom domain (not *.onrender.com), also update the
//  <allow-navigation> host in config.xml so the WebView is allowed to load it.
// ============================================================================
window.APP_CONFIG = {
    APP_URL: "https://kirana-konnect.onrender.com"
};
